# English TTS Model

English voice model assets mounted into the TTS service container.

## Files

- `config.json`: Model configuration.
- `vocab.json`: Token vocabulary.

The service uses these files when `language` is set to English.
