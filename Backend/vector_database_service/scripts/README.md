# Vector Database Scripts

SQL helper scripts for PostgreSQL and pgvector setup.

## Files

- `init.sql`: Enables the pgvector extension.
- `create_hnsw_index.sql`: Creates an HNSW index for faster vector similarity search.

Run these against the PostgreSQL database when manual initialization or index repair is needed.
